import 'package:flutter/material.dart';

class HomePensionesPage extends StatelessWidget {
  const HomePensionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('home Pensiones'));
  }
}
