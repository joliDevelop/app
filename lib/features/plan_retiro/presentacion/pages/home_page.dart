import 'package:flutter/material.dart';

class HomePlanRetiro extends StatelessWidget {
  const HomePlanRetiro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('home Plan de Retiro'));
  }
}
