import 'package:flutter/material.dart';

class HomeSegurosPage extends StatelessWidget {
  const HomeSegurosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('home seguros'));
  }
}
