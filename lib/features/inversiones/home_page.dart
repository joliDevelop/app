import 'package:flutter/material.dart';

class HomeInversiones extends StatelessWidget {
  const HomeInversiones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('home Inversiones'));
  }
}
